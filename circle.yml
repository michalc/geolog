machine:
  node:
    version: 6.9.1
dependencies:
  override:
    - npm install
test:
  override:
    - npm run test
deployment:
  production:
    branch: master
    commands:
      - NODE_ENV=production npm run deploy
