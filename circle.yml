machine:
  node:
    version: 6.9.1
dependencies:
  override:
    - npm install
test:
  override:
    - npm run test-$([ ! -z $CI_PULL_REQUEST ] && echo "pr" || echo "master")
deployment:
  production:
    branch: master
    commands:
      - NODE_ENV=production npm run deploy-master
