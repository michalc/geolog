machine:
  node:
    version: 6.9.1
dependencies:
  override:
    - npm install
test:
  override:
    - npm run ci-test
deployment:
  production:
    branch: master
    commands:
      - npm run ci-deploy
